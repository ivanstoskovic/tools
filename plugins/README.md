# Stoleus Plugins

This is the single discovery root for Stoleus plugins.

Planned plugin categories:

- `modules/`
- `providers/`
- `commands/`
- `generators/`

Each plugin will be self-contained and will include a manifest, implementation,
documentation, and tests.
